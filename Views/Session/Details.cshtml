@model DM_helper.Models.Session

@{
    ViewData["Title"] = "Details";
}

<h1>@Html.DisplayFor(model => model.Name)</h1>

<div>
    <div class="container-fluid">
        <div class="d-flex flex-row flex-wrap">
            <div class="d-flex flex-column">
                <div class="d-flex flex-row">
                    @Html.DisplayNameFor(model => model.Notes)
                </div>
                <div class="d-flex flex-row">
                    @Html.DisplayFor(model => model.Notes)
                </div>
                <div class="d-flex flex-row">
                    <a asp-action="Edit" asp-route-id="@Model.ID">Edit</a> |
                    <a asp-action="Index">Back to List</a>
                </div>
            </div>
            <div class="d-flex flex-column flex-fill">
                <div class="container-fluid">
                    <div class="d-flex flex-row flex-fill">
                        <div class="d-flex flex-column flex-fill">
                            <a asp-action="Create" asp-controller="Encounter">Create Encounter</a>
                            @foreach (var e in Model.Encounters)
                            {
                                <div class="d-flex flex-row w-100">
                                    <div class="d-flex flex-column flex-fill">
                                        Encounters 
                                        <br />
                                        @Html.DisplayFor(modelItem => modelItem.Name) 
                                        <br />
                                        @Html.ActionLink("Bind Char", "Create", "CharacterEncounter", new { EncounterID = e.ID }, new { Class = "d-flex flex-fill a" })
                                    </div>
                                    <div class="d-flex flex-column flex-fill">
                                        <u>Characters</u>
                                        @foreach (var encounter in e.CharacterEncounter)
                                        {
                                            <div class="d-flex flex-row">
                                                <div class="d-flex flex-column">
                                                    @encounter.Character.Name
                                                </div>
                                                <div class="d-flex flex-column">
                                                    @foreach (var w in encounter.Character.Weapon)
                                                    {
                                                        <div class="d-flex flex-row">
                                                            @w.Name |
                                                            @w.Damage |
                                                            @w.Range
                                                            @Html.ActionLink("Roll Damage", "RollDamage", "Encounter", new { damage = w.Damage, CharacterID = w.Character.ID, EncounterID = encounter.ID, Attribute = w.Attribute }, new { Class = "btn btn primary" })
                                                        </div>
                                                    }
                                                </div>
                                            </div>
                                        }
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>