@model DM_helper.Models.CharacterEncounter

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>CharacterEncounter</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="EncounterID" class="control-label">Name</label>
                <select asp-for="EncounterID" class="form-control" asp-items="ViewBag.ID"></select>
            </div>
            <div class="form-group">
                <label asp-for="CharacterID" class="control-label"></label>
                <select asp-for="CharacterID" class="form-control" asp-items="ViewBag.CharacterID"></select>
                <span asp-validation-for="CharacterID" class="text-danger"></span>
            </div>
            <div class="form-group">
                @Html.ActionLink("Create", "Create", "CharacterEncounter", new { SessionID = ViewBag.SessionID }, new { Class = "btn btn-primary" })
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}